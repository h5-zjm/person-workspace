# 股权交易中心 uni-app

基于 Vue 3 + TypeScript + Vite 的 uni-app 项目，目标平台为 H5 和微信小程序。

```bash
pnpm install
pnpm dev:h5
pnpm build:mp-weixin
```

默认 API 地址为 `http://127.0.0.1:8080`，可通过 `VITE_API_BASE_URL` 覆盖。
